import { type StorageAdapter } from 'grammy';
import { SessionDelegate } from './types/SessionDelegate';
export * from './types/SessionDelegate';
export declare class PrismaAdapter<T> implements StorageAdapter<T> {
    private sessionDelegate;
    constructor(repository: SessionDelegate);
    read(key: string): Promise<T>;
    write(key: string, data: T): Promise<void>;
    delete(key: string): Promise<void>;
}
